== 3.11. In the bakery algorithm (Figure 3.11), the values ol turn are unbounded if there is always at least one process in its critical section. Assume reads and writes are atomic. Is it possible to modify the algorithm so that values of turn are always bounded? If so, give a modified algorithm. If not, explain why not.

=== Resposta
